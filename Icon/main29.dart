import 'package:flutter/material.dart';

import 'HomeActivity29.dart';

void main() {
  runApp(MyApp29());
}

class MyApp29 extends StatelessWidget {
  const MyApp29({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Icon',
      home: HomeActivity29(),
      debugShowCheckedModeBanner: false,
    );
  }
}
